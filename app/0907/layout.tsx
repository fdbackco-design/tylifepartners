import type { Metadata } from "next";
import "./0907.css";

export const metadata: Metadata = {
  title: "FEED LIFE | 보험은 계속, 고객 접점은 하나 더",
  description:
    "보험을 버리는 제안이 아닙니다. 여행과 건강, 라이프케어라는 새로운 대화 카드를 더하는 방식입니다.",
};

export default function Landing0907Layout({ children }: { children: React.ReactNode }) {
  return children;
}
